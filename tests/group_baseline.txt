# Group-parity baseline: (file, tag, family) whose group differs from ExifTool.
# New deltas fail regression_tag_groups; fixes tighten it.
# Oracle: tests/expected_groups/*.grps, produced by scripts/gen_group_baselines.sh
# Regenerate: UPDATE_GROUP_BASELINE=1 cargo test --release --test regression regression_tag_groups
# Format: <file>\t<tag>\t<family1|family2>
#
# The single remaining delta, and a downstream consequence of the deliberate
# encoding divergence documented in multiset_baseline.txt (owner decision
# 2026-07-23): our PSP Copyright is read from the PSP block, ExifTool's from IFD0.
# Reaching family-1 parity here means adopting ExifTool's emission order, which
# would surface the raw-0xA9 (invalid-UTF-8) value we deliberately decode instead.
# Left as-is with the encoding choice.
PSP.psp	Copyright	family1
